@XmlSchema( xmlns = {
                        @XmlNs(prefix = "d2l", namespaceURI = "my.custom.namespace")
                    }
          )
package quiz;
import javax.xml.bind.annotation.XmlNs;
import javax.xml.bind.annotation.XmlSchema;
